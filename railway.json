{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "go build -o bin/main cmd/server/main.go"
  },
  "deploy": {
    "startCommand": "./bin/main",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}